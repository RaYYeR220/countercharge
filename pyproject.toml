[project]
name = "countercharge-workspace"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["countercharge-engine[build]"]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["engine", "core"]

[tool.uv.sources]
countercharge-engine = { workspace = true }
